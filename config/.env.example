# =============================================================================
# TesisAIOps - Plantilla de secretos (.env)
# -----------------------------------------------------------------------------
# Copia este archivo a "config/.env" y rellena los valores.
# NUNCA versionar config/.env (está en .gitignore) — ADR-007 / ADR-008.
#
# En la Fase 1 (parser) NO se necesita ningún secreto: este archivo es solo una
# plantilla para las fases de embeddings (2) y LLM (4).
# =============================================================================

# Credencial del proveedor de embeddings / LLM (fases futuras)
# API_KEY=tu_clave_aqui
