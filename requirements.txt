# =============================================================================
# TesisAIOps - Dependencias (Fase 1: Parser)
# -----------------------------------------------------------------------------
# La Fase 1 (parser de logs HAProxy/IIS) se mantiene deliberadamente ligera:
# el parser usa solo la librería estándar de Python (re, json, datetime...).
# Las únicas dependencias externas son la carga de configuración (ADR-008) y
# el framework de pruebas.
#
# El Chunker (Fase 2B, ADR-011) NO añade dependencias: usa solo la stdlib.
# Las dependencias de RAG/LLM (embeddings, vector store, SDK del LLM) se
# añadirán en sus fases correspondientes (2C/2D y 4), no antes.
# =============================================================================

PyYAML>=6.0        # Carga de config/config.yaml (ADR-008)
pytest>=7.4        # Pruebas unitarias (tests/)
